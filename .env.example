# ─────────────────────────────────────────────
# MCP Gateway – Environment Variables
# Copy to .env and fill in values before starting.
# Run ./1password-export.sh to auto-populate from 1Password where possible.
# ─────────────────────────────────────────────

# iMessage
# Path to the custom fastmcp iMessage server script.
IMESSAGE_SCRIPT_PATH=/path/to/imessage-query-server.py

# Obsidian (via Local REST API plugin — Obsidian must be running with plugin enabled)
# Plugin: https://github.com/coddingtonbear/obsidian-local-rest-api
OBSIDIAN_API_KEY=
OBSIDIAN_HOST=localhost
OBSIDIAN_PORT=27123

# Things 3
# From Things 3 → Settings → General → Enable Things URLs → Manage (copy token).
THINGS3_AUTH_TOKEN=

# Slack — OAuth prompt on first connect via mcp.slack.com/mcp (no token needed here)

# GitHub — token pulled automatically from `gh auth token` at startup.
# Just ensure `gh auth login` has been run on this machine. No token stored here.

# Linear — OAuth prompt on first connect via mcp.linear.app/mcp (no token needed here)

# Notion — OAuth prompt on first connect via mcp.notion.com/mcp (no token needed here)

# Honeycomb — OAuth prompt on first connect via mcp.honeycomb.io/mcp (no token needed here)

# Home Assistant
HOMEASSISTANT_URL=http://homeassistant.local:8123
HOMEASSISTANT_TOKEN=
